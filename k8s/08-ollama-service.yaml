apiVersion: v1
kind: Service
metadata:
  name: ollama-service
  namespace: madace
  labels:
    app: ollama
spec:
  type: ClusterIP  # Internal service only
  ports:
    - name: http
      port: 11434
      targetPort: 11434
      protocol: TCP
  selector:
    app: ollama
